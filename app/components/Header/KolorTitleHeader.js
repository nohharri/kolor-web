import styled from 'styled-components';

/**
 * The header that holds the logo and title of the app.
 */
export default styled.div`
  overflow: hidden;
  white-space: nowrap;
  display: flex;
  align-items: center;
`;
