import { COLORS } from '../../utils/constants';

const styles = {
    kolorLogo: {
        width: 100,
        height: 100,
        float: 'left',
    },
    navbarActive: {
        color: COLORS.LIGHT_RED,
    },
}

export default styles;